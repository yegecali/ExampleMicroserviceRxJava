server:
  port: 8888
spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yegecali/ExampleMicroserviceRxJava.git
          search-paths: ConfigRepository
          default-label: feature-gateway